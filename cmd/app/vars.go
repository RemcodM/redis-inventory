package app

var (
	output, outputParams, separators, pattern, password string
	maxChildren, scanCount, throttleNs                  int
	logLevel                                            string
	isCluster                                           bool
)
